/**
 * Feature flags — flip a value here to enable/disable features across the entire app.
 * No rebuilding or component changes needed.
 */

/**
 * Controls the Income CTA block shown below calculator results.
 * Set to `true` when the monetisation layer is ready.
 */
export const SHOW_INCOME_CTA = false;
